# ---- data (1 GB+ scenes, model weights - regeneratable) ----
# everything under data/ is regeneratable EXCEPT the curated demo upload
# set (data/demo_upload/) which is committed so every teammate + the demo
# machine gets the exact same test images.
data/*
!data/demo_upload/
!data/demo_upload/**
backend/models/*.onnx
*.onnx
Unconfirmed*.crdownload
*.zip
!data/demo_upload/product_pds4_demo.zip

# ---- secrets ----
.env
backend/.env
frontend/.env
_test_integrations.py


# ---- python ----
__pycache__/
*.pyc
.venv/

# ---- node/vercel ----
node_modules/
.vercel/
